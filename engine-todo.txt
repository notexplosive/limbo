Loading screen
    - Create a loader
    - Queue up all the things we want to load
    - Render a loading screen that fills over time
    - "On loader completion, run all objects and use PIXI.Texture.from() to pull the loaded textures out of the texture cache" -- from pixi documentation, we might need to do this?

PrimitiveRenderer
    - Draw a line
    - Draw a rectangle
    - Draw a circle
    - Draw a polygon